<li class="field <% if (error) { %>error<% } %>">
  <label for="<%= name %>">What would you like to know?</label>
  <div class="textarea">
  <textarea name="contact[<%= name %>]"></textarea>
  </div>
</li>
